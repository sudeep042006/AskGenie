# 🏗️ Delete Bot Feature - Architecture & Visual Guide

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        ASKGENIE APPLICATION                     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    App.jsx                               │ │
│  │  ┌────────────────────────────────────────────────────┐ │ │
│  │  │ handleDeleteBot(botId)                             │ │ │
│  │  │  - Call chatbotApi.deleteBot(botId)               │ │ │
│  │  │  - Remove bot from bots state                      │ │ │
│  │  │  - Clear activeBot if needed                       │ │ │
│  │  │  - Handle errors                                   │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │
│  │           ↓ Pass prop                                   │ │
│  │  ┌────────────────────────────────────────────────────┐ │ │
│  │  │  <Sidebar onDeleteBot={handleDeleteBot} />         │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              Sidebar.jsx                                │ │
│  │  ┌────────────────────────────────────────────────────┐ │ │
│  │  │ [BOT CARD] (Hover)                                │ │ │
│  │  │  Name: example.com      [🗑️]                      │ │ │
│  │  │  Domain: example.com    (Delete Button)            │ │ │
│  │  │  Status: 🟢 Ready                                 │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │
│  │                    ↓ Click                             │ │
│  │  ┌────────────────────────────────────────────────────┐ │ │
│  │  │ ❓ Confirmation Dialog                             │ │ │
│  │  │ "Are you sure you want to delete this chatbot?    │ │ │
│  │  │  This action cannot be undone."                   │ │ │
│  │  │                                                    │ │ │
│  │  │  [Cancel]  [OK]                                   │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │
│  │                    ↓ Confirm                            │ │
│  │  ┌────────────────────────────────────────────────────┐ │ │
│  │  │ handleDeleteBot(botId, event)                      │ │ │
│  │  │  - window.confirm() → Check if user confirmed     │ │ │
│  │  │  - setDeletingId(botId) → Show spinner            │ │ │
│  │  │  - await onDeleteBot(botId) → Call parent handler │ │ │
│  │  │  - Catch errors → Show error banner               │ │ │
│  │  │  - setDeletingId(null) → Hide spinner             │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │            api.js (API Service Layer)                   │ │
│  │  ┌────────────────────────────────────────────────────┐ │ │
│  │  │ chatbotApi.deleteBot(chatbotId)                    │ │ │
│  │  │  return await api.delete(                          │ │ │
│  │  │    `/chatbot/${chatbotId}`                         │ │ │
│  │  │  )                                                 │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                             ↓ HTTP
        ┌──────────────────────────────────────────────┐
        │ DELETE /api/chatbot/[chatbotId]              │
        │ Content-Type: application/json               │
        └──────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (Express.js)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │            apiRoutes.js                                 │ │
│  │  router.delete('/chatbot/:chatbotId',                   │ │
│  │    deleteChatbot                                        │ │
│  │  )                                                      │ │
│  └──────────────────────────────────────────────────────────┘ │
│                    ↓                                            │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │       crawlController.js                                │ │
│  │       deleteChatbot(req, res)                           │ │
│  │                                                          │ │
│  │  ✓ Get chatbotId from request params                   │ │
│  │  ✓ Validate chatbotId exists                           │ │
│  │                                                          │ │
│  │  STEP 1: Delete from MongoDB                           │ │
│  │  ├─ Chatbot.findByIdAndDelete(chatbotId)              │ │
│  │  ├─ Check if bot exists (404 if not)                  │ │
│  │  └─ Console: "✅ MongoDB Record Deleted"               │ │
│  │                                                          │ │
│  │  STEP 2: Delete from Supabase                          │ │
│  │  ├─ supabase.from('documents').delete()                │ │
│  │  ├─ .eq('metadata->chatbot_id', chatbotId)            │ │
│  │  ├─ Delete all vectors linked to this bot             │ │
│  │  └─ Console: "🗑️ Supabase Vectors Deleted"             │ │
│  │                                                          │ │
│  │  STEP 3: Return Response                               │ │
│  │  └─ res.json({                                         │ │
│  │      message: "Chatbot deleted successfully!",         │ │
│  │      chatbotId: "...",                                 │ │
│  │      deletedBot: { ... }                               │ │
│  │    })                                                   │ │
│  │                                                          │ │
│  │  ERROR HANDLING:                                        │ │
│  │  ├─ 400: Missing chatbotId                             │ │
│  │  ├─ 404: Chatbot not found                             │ │
│  │  ├─ 500: MongoDB delete fails                          │ │
│  │  └─ 500: Supabase delete fails                         │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                             ↓ HTTP
        ┌──────────────────────────────────────────────┐
        │ Response 200 OK                              │
        │ {                                            │
        │   "message": "Chatbot deleted successfully!", │
        │   "chatbotId": "507f1f77bcf86cd799439011",   │
        │   "deletedBot": { ... }                      │
        │ }                                            │
        └──────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React) RESPONSE                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  App.jsx handleDeleteBot() receives response                   │
│  ├─ setBots(prev => prev.filter(bot => bot._id !== botId))   │
│  ├─ setActiveBot(null) if bot was active                      │
│  ├─ Console: "🗑️ Chatbot deleted: [botId]"                    │
│  └─ UI Updates Automatically                                  │
│                                                                 │
│  Sidebar.jsx updates:                                          │
│  ├─ Bot card fades out                                        │
│  ├─ List scrolls up if needed                                 │
│  ├─ Spinner disappears                                        │
│  └─ Delete button becomes unavailable                         │
│                                                                 │
│  Result: Bot no longer visible in sidebar                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

```

---

## Delete Flow Sequence Diagram

```
User          Browser              API Server         MongoDB        Supabase
 │              │                     │                 │               │
 │ Click Delete │                     │                 │               │
 ├─────────────>│                     │                 │               │
 │              │ Show Confirmation   │                 │               │
 │              │<────────────────────┤                 │               │
 │ Confirm      │                     │                 │               │
 ├─────────────>│                     │                 │               │
 │              │ Show Spinner        │                 │               │
 │              │ (Loading State)     │                 │               │
 │              │                     │                 │               │
 │              │ DELETE /api/chatbot/:id               │               │
 │              ├────────────────────>│                 │               │
 │              │                     │                 │               │
 │              │                  findByIdAndDelete    │               │
 │              │                  ────────────────────>│               │
 │              │                     │<────────────────│               │
 │              │                     │ ✅ Deleted      │               │
 │              │                     │                 │               │
 │              │                  delete (vectors)    │               │
 │              │                  ────────────────────────────────────>│
 │              │                     │                 │               │
 │              │                     │<───────────────────────────────│
 │              │                     │ ✅ Vectors Deleted             │
 │              │                     │                 │               │
 │              │<────── 200 OK ──────│                 │               │
 │              │ {success response}  │                 │               │
 │              │                     │                 │               │
 │              │ Update State:       │                 │               │
 │              │ - Remove bot        │                 │               │
 │              │ - Close chat        │                 │               │
 │              │ - Hide spinner      │                 │               │
 │              │                     │                 │               │
 │<─ Hide Spinner─                   │                 │               │
 │<─ Bot Removed │                    │                 │               │
 │              │                     │                 │               │
```

---

## State Management Flow

```
┌─────────────────────────────────────┐
│         INITIAL STATE               │
├─────────────────────────────────────┤
│ bots: [                             │
│   { _id: "bot123", name: "Bot 1" }, │
│   { _id: "bot456", name: "Bot 2" }  │
│ ]                                   │
│ activeBot: null                     │
│ error: null                         │
└─────────────────────────────────────┘
           ↓ User hovers on bot456
┌─────────────────────────────────────┐
│    SIDEBAR STATE CHANGES            │
├─────────────────────────────────────┤
│ hoveredBot: "bot456"                │
│ (Delete button becomes visible)     │
└─────────────────────────────────────┘
           ↓ User clicks delete
┌─────────────────────────────────────┐
│  CONFIRMATION DIALOG APPEARS        │
├─────────────────────────────────────┤
│ (window.confirm shows)              │
└─────────────────────────────────────┘
           ↓ User confirms
┌─────────────────────────────────────┐
│    LOADING STATE BEGINS             │
├─────────────────────────────────────┤
│ deletingId: "bot456"                │
│ deleteError: null                   │
│ (Spinner appears on button)         │
└─────────────────────────────────────┘
           ↓ API call in progress
┌─────────────────────────────────────┐
│   BACKEND PROCESSING                │
├─────────────────────────────────────┤
│ - Delete from MongoDB               │
│ - Delete from Supabase              │
│ - Return success response           │
└─────────────────────────────────────┘
           ↓ API returns 200 OK
┌─────────────────────────────────────┐
│    STATE UPDATE (SUCCESS)           │
├─────────────────────────────────────┤
│ bots: [                             │
│   { _id: "bot123", name: "Bot 1" }  │
│ ]                                   │
│ deletingId: null                    │
│ deleteError: null                   │
│ activeBot: null (cleared if needed) │
│                                     │
│ (Bot456 removed from sidebar)       │
└─────────────────────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────────────┐
│      API Call to DELETE             │
└─────────────────────────────────────┘
           ↓
       ┌───┴───┐
       ↓       ↓
   SUCCESS   ERROR
       │       │
       │   ┌───┴───────────────┐
       │   ↓                   ↓
       │ Network Error    Server Error (4xx/5xx)
       │   │                   │
       │   ├─ Catch error      ├─ Check status
       │   │                   │
       │   └─ setDeleteError() ├─ Extract message
       │      └─ Show Banner   │  from response
       │         └─ 5s timeout └─ setDeleteError()
       │                          └─ Show Banner
       │                             └─ 5s timeout
       │
       └─ setBots() filter
          └─ setActiveBot(null)
             └─ setDeletingId(null)
                └─ UI Updates
```

---

## Component Props Flow

```
┌──────────────────────────────────────────────────┐
│              App.jsx                             │
│  ┌─────────────────────────────────────────────┐ │
│  │ State:                                      │ │
│  │ - bots: Bot[]                              │ │
│  │ - activeBot: Bot | null                    │ │
│  │ - error: string | null                     │ │
│  │                                             │ │
│  │ Handler:                                    │ │
│  │ - handleDeleteBot(botId)                   │ │
│  └─────────────────────────────────────────────┘ │
│              │                                   │
│              └─> Props to Sidebar:              │
│                  - bots                         │
│                  - onSelectBot                  │
│                  - onNewChat                    │
│                  - onDeleteBot <── DELETE       │
│                                                  │
└──────────────────────────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────────────────┐
│          Sidebar.jsx                             │
│  ┌─────────────────────────────────────────────┐ │
│  │ Props Received:                             │ │
│  │ - bots: Bot[]                              │ │
│  │ - onSelectBot: (bot: Bot) => void          │ │
│  │ - onNewChat: () => void                    │ │
│  │ - onDeleteBot: (botId: string) => void     │ │
│  │                                             │ │
│  │ Local State:                                │ │
│  │ - hoveredBot: string | null                │ │
│  │ - deletingId: string | null                │ │
│  │ - deleteError: string | null               │ │
│  │                                             │ │
│  │ Handler:                                    │ │
│  │ - handleDeleteBot(botId, e)                │ │
│  │   └─ await onDeleteBot(botId)              │ │
│  └─────────────────────────────────────────────┘ │
│              │                                   │
│              └─> Delete Button:                 │
│                  onClick → handleDeleteBot      │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

## HTTP Request/Response Details

### Success Case

**Request:**

```http
DELETE /api/chatbot/507f1f77bcf86cd799439011 HTTP/1.1
Host: localhost:3000
Content-Type: application/json
User-Agent: Axios/1.4.0
Connection: keep-alive
```

**Response:**

```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 256
Connection: keep-alive

{
  "message": "Chatbot deleted successfully!",
  "chatbotId": "507f1f77bcf86cd799439011",
  "deletedBot": {
    "_id": "507f1f77bcf86cd799439011",
    "userId": "cb1ed123-08da-4562-b36b-299f726eabe0",
    "url": "https://example.com",
    "name": "example.com",
    "status": "ready",
    "createdAt": "2025-12-18T12:00:00.000Z",
    "__v": 0
  }
}
```

### Error Cases

**Case 1: Missing chatbotId**

```http
HTTP/1.1 400 Bad Request

{
  "error": "chatbotId is required"
}
```

**Case 2: Chatbot not found**

```http
HTTP/1.1 404 Not Found

{
  "error": "Chatbot not found"
}
```

**Case 3: Server error**

```http
HTTP/1.1 500 Internal Server Error

{
  "error": "Failed to delete chatbot"
}
```

---

## Database Changes Visualization

### Before Deletion

**MongoDB (chatbots collection):**

```
┌─────────────────────────────────────┐
│ _id: 507f1f77bcf86cd799439011      │
│ userId: "cb1ed123-..."              │
│ url: "https://example.com"          │
│ name: "example.com"                 │
│ status: "ready"                     │
│ createdAt: 2025-12-18T12:00:00Z     │
└─────────────────────────────────────┘
```

**Supabase (documents table):**

```
┌────────────────────────────────────────────┐
│ id: 1                                      │
│ content: "Page content..."                 │
│ embedding: [0.123, 0.456, ...]            │
│ metadata: {                                │
│   chatbot_id: "507f1f77bcf86cd799439011", │
│   user_id: "cb1ed123-...",                │
│   url: "https://example.com"              │
│ }                                          │
└────────────────────────────────────────────┘
┌────────────────────────────────────────────┐
│ id: 2                                      │
│ content: "More content..."                 │
│ embedding: [0.789, 0.101, ...]            │
│ metadata: {                                │
│   chatbot_id: "507f1f77bcf86cd799439011", │
│   user_id: "cb1ed123-...",                │
│   url: "https://example.com"              │
│ }                                          │
└────────────────────────────────────────────┘
```

### After Deletion

**MongoDB (chatbots collection):**

```
❌ Document completely removed
   (No entry in collection)
```

**Supabase (documents table):**

```
❌ All vectors with matching metadata->chatbot_id removed
   (No entries with "507f1f77bcf86cd799439011" remain)
```

---

## Timeline for Delete Operation

```
Time        Event                                  Duration
────────────────────────────────────────────────────────
T+0ms       User clicks delete button              -
T+100ms     Confirmation dialog appears           100ms
T+500ms     User confirms deletion                400ms
T+510ms     Spinner appears, button disabled      10ms
T+520ms     API request sent to backend           10ms
T+550ms     Backend receives request              30ms
T+600ms     MongoDB deletion completes            50ms
T+650ms     Supabase deletion completes           50ms
T+660ms     Backend returns 200 OK response       10ms
T+670ms     Frontend receives response            10ms
T+680ms     Frontend state updated                10ms
T+690ms     Spinner disappears, bot removed       10ms
────────────────────────────────────────────────────────
Total Time: ~690ms (less than 1 second)
```

---

**Architecture Documentation:** Complete  
**Status:** 🟢 Ready for Development & Testing
